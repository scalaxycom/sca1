---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kyverno
resources:
  - namespace.yaml
  - helmrepository.yaml
  - release.yaml
configMapGenerator:
  - name: kyverno-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml